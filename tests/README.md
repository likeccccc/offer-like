# Phoenix 回归检查

## v3.14

- `node tests/radio-fill.cjs`：视觉隐藏的原生单选输入按可见组扫描；性别点击后核验 checked；不改写选项 value。
- `node tests/phoenix-regression.cjs`：新增福建省漳州市与福建/漳州的路径匹配；同一浮层内兄弟确认按钮；完整毕业日期选择；缺少日时不造日期。
- `node tests/portrait-upload.cjs`：页面原生文件 setter、隔离环境兼容路径、300 KB 边界及真实字节数检查；交付成功不等于服务器上传成功。

实际照片失败原因仍需页面返回结果确认；v3.14 日志新增桥接版本、交付错误原因和短时可见的上传错误提示，不输出照片内容。页面超过观察时间才报错仍需手动查看。

运行 `node tests/phoenix-regression.cjs`。

v3.13 修复依据：用户提供的 v3.12 日志中，AI 模板请求耗时 0.8 秒，整轮 850.5 秒。年份选择失败后持续翻页；学历列表被残留日历误分类；籍贯和出生日期未生成填写值。

模拟用例覆盖：年份 DOM 保留时转入月份、翻页无变化时停止、学历列表不读取全局残留日历、成功和失败均关闭面板、年月日严格读回、地区逐级导航与末级选中。既有模板、经历分组及等待逻辑测试也已运行。

这些检查不代表 Edge 真机验收。重新加载扩展并重新打开招聘表单后，重点检查两段教育日期、两段学历、个人最高学历、毕业时间和籍贯。日志版本应为 v3.13。若显示“资料库字段为空: hometown / birthday”，需核对资料库对应字段；不从生源地或地址猜测籍贯。保留“AI 模板耗时”“扫描与展开”“本轮总计”用于比较真实耗时。
